import {type AppIconName} from '../../components/icon/iconNames';

export type RuleEntry = {
    icon: AppIconName;
    title: string;
    body: string;
};

export type RulesHeroCopy = {
    title: string;
    body: string;
};
